name: NovelDokusha Release APK (signed + publish)

on:
  workflow_dispatch:
  push:
    tags:
      - 'v*'     # runs automatically when you push a tag like v1.0.0

permissions:
  contents: write   # needed to create/update releases

jobs:
  build-and-release:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up JDK 17
        uses: actions/setup-java@v4
        with:
          java-version: '17'
          distribution: 'temurin'
          cache: 'gradle'

      - name: Set up Android SDK
        uses: android-actions/setup-android@v3

      - name: Make Gradle executable
        run: chmod +x ./gradlew

      - name: Generate release keystore
        run: |
          keytool -genkeypair \
            -alias noveldokushaKey \
            -keyalg RSA \
            -keysize 2048 \
            -validity 10000 \
            -storepass password \
            -keypass password \
            -dname "CN=NovelDokusha,O=Self,C=US" \
            -keystore noveldokusha-release.keystore

      - name: Build signed release APK
        run: |
          ./gradlew assembleRelease --no-daemon \
            -Pandroid.injected.signing.store.file=$PWD/noveldokusha-release.keystore \
            -Pandroid.injected.signing.store.password=password \
            -Pandroid.injected.signing.key.alias=noveldokushaKey \
            -Pandroid.injected.signing.key.password=password

      - name: Upload signed release APK artifact
        uses: actions/upload-artifact@v4
        with:
          name: noveldokusha-signed-release-apk
          path: app/build/outputs/apk/**/*.apk

      - name: Publish GitHub Release with APK
        uses: softprops/action-gh-release@v2
        with:
          files: app/build/outputs/apk/**/*.apk
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
